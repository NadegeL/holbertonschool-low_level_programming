#include "main.h"
/**
 * int _abs - Checks absolute value.
 * @r: The number checked.
 *
 * print absolute value of int.
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (0);
	}
	else{
		return (-r);
	}
}
